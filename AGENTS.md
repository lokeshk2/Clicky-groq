# Agents

## Cursor Cloud specific instructions

This repository (`Clicky-groq`) is currently an empty/starter project with only a `README.md`. No source code, dependencies, build system, or services exist yet.

- **No services to start**: There are no backend, frontend, or database services to run.
- **No dependencies to install**: There is no `package.json`, `requirements.txt`, `pyproject.toml`, or any other dependency manifest.
- **No lint/test/build commands**: No tooling has been configured yet.
- When source code is added, update this section with startup caveats and non-obvious development notes.
